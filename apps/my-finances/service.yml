apiVersion: v1
kind: Service
metadata:
  name: my-finances
  namespace: my-finances
spec:
  type: NodePort
  selector:
    app: my-finances
  ports:
    - name: web
      port: 8080
      targetPort: 8080
      nodePort: 31081
      protocol: TCP
