apiVersion: v1
kind: Service
metadata:
  name: home-dns-dns
  namespace: home-dns
spec:
  type: LoadBalancer
  selector:
    app: home-dns
  ports:
    - name: dns-udp
      port: 53
      targetPort: 30053
      protocol: UDP
